import React from "react";
import { Dashboard, Header} from "../components";

const HomePage = () =>{
    return(
        <div>
            <Dashboard />
            <Header/>
        </div>
    );
}

export default HomePage;